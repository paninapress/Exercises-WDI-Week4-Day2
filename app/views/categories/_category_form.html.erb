<!-- PARTIAL CATEGORY CREATE FORM -->

<%= form_for(@category) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %><br>
	<%= f.label "Choose products:" %> <br>
	<%= collection_check_boxes :category, :product_ids, Product.all, :id, :name %>
	<br>
	<%= f.submit %>
<% end %>